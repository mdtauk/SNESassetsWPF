<UserControl
    x:Class="SNESassetsWPF.UserControls.WarningSummary"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SNESassetsWPF.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:SNESassetsWPF.Models"
    Padding="0"
    d:DesignHeight="220"
    d:DesignWidth="120"
    mc:Ignorable="d">

    <UserControl.Resources>
        <x:Array x:Key="DesignWarnings" Type="models:UiMessage">
            <models:UiMessage Severity="Info" Text="Welcome to the Microsoft Agent scripting helper. How may I help you?" />
            <models:UiMessage Severity="Warning" Text="SCR file incomplete" />
            <models:UiMessage Severity="Warning" Text="Missing footer" />
            <models:UiMessage Severity="Error" Text="Unreadable SCR file" />
        </x:Array>
    </UserControl.Resources>


    <Border>
        <ScrollViewer
            Margin="0,0,0,0"
            Padding="0"
            BorderThickness="0"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <ItemsControl
                x:Name="itmctlWarningList"
                Margin="4,8,12,8"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Left"
                d:ItemsSource="{StaticResource DesignWarnings}"
                Focusable="False"
                IsHitTestVisible="False">

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Margin" Value="0,0,0,8" />
                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                    </Style>
                </ItemsControl.ItemContainerStyle>


                <!--  Force full-width stacking  -->
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Margin="0" HorizontalAlignment="Stretch" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>


                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock
                            Margin="0"
                            HorizontalAlignment="Stretch"
                            Text="{Binding Text}"
                            TextAlignment="Left"
                            TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Severity}" Value="Warning">
                                            <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCautionBrush}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Severity}" Value="Error">
                                            <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCriticalBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

    </Border>
</UserControl>
